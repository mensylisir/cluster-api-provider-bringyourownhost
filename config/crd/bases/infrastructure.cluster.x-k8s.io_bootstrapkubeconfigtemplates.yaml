---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: bootstrapkubeconfigtemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: BootstrapKubeconfigTemplate
    listKind: BootstrapKubeconfigTemplateList
    plural: bootstrapkubeconfigtemplates
    shortNames:
    - bkt
    singular: bootstrapkubeconfigtemplate
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: BootstrapKubeconfigTemplate is the Schema for the bootstrapkubeconfigtemplates
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BootstrapKubeconfigTemplateSpec defines the desired state
              of BootstrapKubeconfigTemplate
            properties:
              template:
                description: Template is the specification of the desired behavior
                  of the BootstrapKubeconfig.
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    type: object
                  labels:
                    additionalProperties:
                      type: string
                    type: object
                  spec:
                    description: Spec is the specification of the desired behavior
                      of the BootstrapKubeconfig.
                    properties:
                      apiserver:
                        description: APIServer is the address of the kubernetes cluster
                          (https://hostname:port).
                        type: string
                      certificate-authority-data:
                        description: CertificateAuthorityData contains PEM-encoded
                          certificate authority certificates.
                        type: string
                      insecure-skip-tls-verify:
                        default: false
                        description: InsecureSkipTLSVerify skips the validity check
                          for the server's certificate. This will make your HTTPS
                          connections insecure.
                        type: boolean
                    required:
                    - apiserver
                    - certificate-authority-data
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
          status:
            description: BootstrapKubeconfigTemplateStatus defines the observed state
              of BootstrapKubeconfigTemplate
            properties:
              capacity:
                additionalProperties:
                  anyOf:
                  - type: integer
                  - type: string
                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                  x-kubernetes-int-or-string: true
                description: Capacity defines the resource capacity for this machine
                  template.
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
